import React from 'react';

const ProjectListItem = (props) => {
  return (
    <div>Project Item will go here and it may be very large and need lots of width space</div>
  )
}

export default ProjectListItem;